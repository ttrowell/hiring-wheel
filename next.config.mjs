/** @type {import('next').NextConfig} */
//const nextConfig = {};
const nextConfig = {
    //distDir: "build",
    reactStrictMode: true,
    swcMinify: true,
};


export default nextConfig;
//module.exports = nextConfig